
//Arrow Function
//1. named function as a variable 'const add'
//2. take parameter by '(first,second)'
//3. return value by '=> first + second;'
const add = (first,second) => first + second;
const result = add(30,20);
console.log(result);

console.log('-------------------------------------------------------------');

const add1 = (first,second) => first + second;
const getFullName = (first, last) => first + ' '+ last;
const multiply = (a,b) => a*b;
const result1 = multiply(7, 8);
console.log(result1);


console.log('-------------------------------------------------------------');

const add2 = (a,b,c,d,e) => a+b+c+d+e; //many parameter
const getPie = () => 3.1416; //if there in no parameter
const fiveTimes = number => number * 5; //single parameter




